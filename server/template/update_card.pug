extends include/layout.pug

block content
    h1 Update Card 

    form(method='POST')
        label(for="new_id") Last Name
        input#new_id(type="text", placeholder="Enter UID" name="new_id", value=card_id required)
        br
        button(type="submit") Update Card
